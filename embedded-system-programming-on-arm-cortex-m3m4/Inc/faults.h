#ifndef FAULTS_H
#define FAULTS_H

/* Lesson: exception entry/exit & faults — the stacked frame read back,
 * all four fault handlers live, recovery by editing the frame,
 * escalation to HardFault on purpose. */
void playing_with_faults(void);

#endif
