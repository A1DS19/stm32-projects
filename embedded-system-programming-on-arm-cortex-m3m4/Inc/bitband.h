#ifndef BITBAND_H
#define BITBAND_H

/* Lesson: bit-banding — the alias regions that turn one bit into one
 * addressable word; exercise s76 done on a linker-placed variable. */
void playing_with_bit_banding(void);

#endif
