/* Lesson: unions — overlap, float bits (IEEE-754), and tagged unions.
 * See Src/union.c. Output on `make serial`. */

#include "union.h"

int main(void)
{
  playing_with_union();

  for (;;) {
  }
}
