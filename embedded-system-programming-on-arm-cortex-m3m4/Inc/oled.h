#ifndef OLED_H
#define OLED_H

/* Side demo: 128x32 SSD1306 OLED over I2C3 — a display with its own
 * controller chip, driven through the bus instead of raw pins. Never
 * returns; wiring (4 pins) in Src/oled.c. */
void playing_with_the_oled(void);

#endif
