#ifndef MEMMAP_H
#define MEMMAP_H

/* Lesson: the fixed Cortex-M 4GB memory map (CODE/SRAM/peripheral/PPB
 * regions), AHB vs APB buses, and where this program actually lives. */
void exploring_the_memory_map(void);

#endif
