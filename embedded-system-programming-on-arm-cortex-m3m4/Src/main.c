/* Lesson: memory map & bus interfaces; see Src/memmap.c. `make serial`.
 * Previous: tbit.c, resetseq.c, inlineasm.c, coreregs.c, access.c, modes.c. */

#include "memmap.h"

int main(void) {
    exploring_the_memory_map();

    for (;;) {}
}
