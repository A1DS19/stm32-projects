#ifndef CMSISNVIC_H
#define CMSISNVIC_H

/* Lesson: the NVIC again, through CMSIS — the day-job dialect proven
 * equivalent (and once superior) to nvic.c's raw register stores. */
void playing_with_cmsis_nvic(void);

#endif
