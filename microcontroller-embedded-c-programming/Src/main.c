/* Lesson: the preprocessor — macros, # and ##, defined(), conditional
 * compilation. The course finale; see Src/preproc.c. `make serial`. */

#include "preproc.h"

int main(void)
{
  playing_with_preproc();

  for (;;) {
  }
}
