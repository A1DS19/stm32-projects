#ifndef SEVENSEG_H
#define SEVENSEG_H

/* Side demo: 3461AS 4-digit 7-segment display multiplexed over raw GPIO
 * registers — the memory-map/register discussion made visible. Never
 * returns; wiring table in Src/sevenseg.c. */
void playing_with_seven_segments(void);

#endif
