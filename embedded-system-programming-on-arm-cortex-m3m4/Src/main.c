/* Lesson: bit-banding; see Src/bitband.c. `make serial`.
 * Side demos parked in git: oled.c (I2C3 SSD1306), sevenseg.c (raw GPIO).
 * Previous lessons: memmap.c, tbit.c, resetseq.c, inlineasm.c,
 * coreregs.c, access.c, modes.c. */

#include "bitband.h"

int main(void) {
    playing_with_bit_banding();

    for (;;) {}
}
