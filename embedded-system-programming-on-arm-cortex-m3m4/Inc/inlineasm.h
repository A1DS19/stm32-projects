#ifndef INLINEASM_H
#define INLINEASM_H

/* Lesson: GCC inline assembly — the asm(template : outputs : inputs :
 * clobbers) contract, plus the slide-39 load/add/store exercise. */
void playing_with_inline_assembly(void);

#endif
