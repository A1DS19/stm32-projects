#ifndef RESETSEQ_H
#define RESETSEQ_H

/* Lesson: the reset sequence — how the core gets from power-on to main,
 * proven by reading the vector table and its boot alias at address 0. */
void playing_with_reset_sequence(void);

#endif
