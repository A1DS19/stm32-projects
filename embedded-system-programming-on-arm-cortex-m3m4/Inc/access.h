#ifndef ACCESS_H
#define ACCESS_H

/* Lesson: access levels — privileged (PAL) vs unprivileged (nPAL) thread
 * mode, proven by touching the NVIC from both and hard-faulting once. */
void playing_with_access_levels(void);

#endif
